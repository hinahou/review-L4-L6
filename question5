<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>國一國文段考衝刺站 🎒</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts: Noto Sans TC & Fredoka One (for cute vibes) -->
    <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;600&family=Noto+Sans+TC:wght@400;500;700&display=swap" rel="stylesheet">
    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>
    <!-- Confetti Effect -->
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>

    <style>
        body {
            font-family: 'Noto Sans TC', sans-serif;
            background-color: #FDF2F8; /* Pink-50 */
            background-image: radial-gradient(#FBCFE8 2px, transparent 2px);
            background-size: 32px 32px;
        }
        .font-cute {
            font-family: 'Fredoka', sans-serif;
        }
        .card-hover {
            transition: all 0.3s ease;
        }
        .card-hover:active {
            transform: scale(0.95);
        }
        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #f1f1f1; 
        }
        ::-webkit-scrollbar-thumb {
            background: #F472B6; 
            border-radius: 10px;
        }
        .animate-pop {
            animation: pop 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }
        @keyframes pop {
            0% { transform: scale(0.5); opacity: 0; }
            100% { transform: scale(1); opacity: 1; }
        }
        .gradient-text {
            background: linear-gradient(to right, #ec4899, #8b5cf6);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
    </style>
</head>
<body class="text-slate-800 min-h-screen flex flex-col items-center justify-center p-4">

    <!-- App Container -->
    <div id="app" class="w-full max-w-md bg-white rounded-3xl shadow-2xl overflow-hidden border-4 border-white relative min-h-[700px] flex flex-col">
        
        <!-- Header -->
        <header class="bg-white p-4 flex justify-between items-center shadow-sm z-10 sticky top-0">
            <div class="flex items-center gap-2">
                <div class="bg-pink-100 p-2 rounded-full text-pink-500">
                    <i data-lucide="book-open-check"></i>
                </div>
                <h1 class="font-bold text-lg text-slate-700">國文衝刺站</h1>
            </div>
            <div class="flex items-center gap-2 bg-yellow-100 px-3 py-1 rounded-full border border-yellow-200 shadow-sm">
                <span class="text-yellow-600 font-bold font-cute text-lg" id="score-display">0</span>
                <i data-lucide="coins" class="w-4 h-4 text-yellow-500"></i>
            </div>
        </header>

        <!-- Main Content Area -->
        <main id="main-content" class="flex-1 overflow-y-auto p-4 relative bg-gradient-to-b from-white to-pink-50">
            
            <!-- VIEW: Home Menu -->
            <div id="view-home" class="flex flex-col items-center gap-6 py-8 animate-pop">
                <div class="text-center space-y-2">
                    <div class="w-32 h-32 bg-gradient-to-br from-pink-300 to-purple-400 rounded-full mx-auto flex items-center justify-center shadow-lg mb-4 relative">
                        <span class="text-6xl filter drop-shadow-md">🎓</span>
                        <div class="absolute -bottom-2 -right-2 bg-white p-2 rounded-full shadow-md">
                            <span class="text-2xl">✏️</span>
                        </div>
                    </div>
                    <h2 class="text-2xl font-bold gradient-text">國一二段複習</h2>
                    <p class="text-slate-500 text-sm">每天 5 題，輕鬆拿高分！</p>
                </div>

                <div class="w-full space-y-4 mt-4">
                    <button onclick="app.startQuiz()" class="w-full bg-slate-800 hover:bg-slate-700 text-white p-4 rounded-2xl shadow-lg flex items-center justify-between group card-hover">
                        <div class="flex items-center gap-3">
                            <div class="bg-slate-700 group-hover:bg-slate-600 p-2 rounded-xl transition-colors">
                                <i data-lucide="play" class="text-green-400"></i>
                            </div>
                            <div class="text-left">
                                <div class="font-bold text-lg">開始練習</div>
                                <div class="text-slate-400 text-xs">隨機 5 題挑戰</div>
                            </div>
                        </div>
                        <i data-lucide="chevron-right" class="text-slate-500"></i>
                    </button>

                    <button onclick="app.showShop()" class="w-full bg-white border-2 border-slate-100 hover:border-pink-200 text-slate-700 p-4 rounded-2xl shadow-sm flex items-center justify-between group card-hover">
                        <div class="flex items-center gap-3">
                            <div class="bg-pink-100 group-hover:bg-pink-200 p-2 rounded-xl transition-colors">
                                <i data-lucide="store" class="text-pink-500"></i>
                            </div>
                            <div class="text-left">
                                <div class="font-bold text-lg">福利社</div>
                                <div class="text-slate-400 text-xs">用點數換獎勵</div>
                            </div>
                        </div>
                        <i data-lucide="chevron-right" class="text-slate-300"></i>
                    </button>
                    
                    <!-- Stats Preview -->
                    <div class="grid grid-cols-2 gap-3 mt-2">
                         <div class="bg-blue-50 p-3 rounded-2xl border border-blue-100 flex flex-col items-center">
                            <span class="text-xs text-blue-400 font-bold uppercase">累積答對</span>
                            <span class="text-2xl font-cute font-bold text-blue-600" id="total-correct">0</span>
                         </div>
                         <div class="bg-purple-50 p-3 rounded-2xl border border-purple-100 flex flex-col items-center">
                            <span class="text-xs text-purple-400 font-bold uppercase">蒐集圖鑑</span>
                            <span class="text-2xl font-cute font-bold text-purple-600" id="unlocked-count">0/6</span>
                         </div>
                    </div>
                </div>
            </div>

            <!-- VIEW: Quiz -->
            <div id="view-quiz" class="hidden h-full flex flex-col">
                <!-- Progress Bar -->
                <div class="w-full bg-slate-100 h-2 rounded-full mb-6">
                    <div id="progress-bar" class="bg-pink-500 h-2 rounded-full transition-all duration-500" style="width: 20%"></div>
                </div>

                <!-- Question Card -->
                <div class="bg-white p-6 rounded-3xl shadow-sm border border-slate-100 mb-6 flex-1 relative overflow-hidden">
                    <div class="absolute -right-4 -top-4 bg-pink-50 w-24 h-24 rounded-full opacity-50"></div>
                    <span class="bg-pink-100 text-pink-600 text-xs font-bold px-2 py-1 rounded-md mb-3 inline-block">題目 <span id="q-current">1</span>/5</span>
                    <h3 id="q-text" class="text-lg font-bold text-slate-800 leading-relaxed mb-4">題目載入中...</h3>
                    
                    <!-- Feedback Area (Initially Hidden) -->
                    <div id="feedback-area" class="hidden mt-4 p-4 bg-blue-50 border border-blue-100 rounded-xl animate-pop">
                        <div class="flex items-start gap-3">
                            <div id="feedback-icon" class="mt-1"></div>
                            <div>
                                <p id="feedback-title" class="font-bold text-sm"></p>
                                <p id="feedback-desc" class="text-sm text-slate-600 mt-1 leading-snug"></p>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Options -->
                <div id="options-container" class="space-y-3 mb-4">
                    <!-- Buttons injected by JS -->
                </div>

                <!-- Next Button -->
                <button id="next-btn" onclick="app.nextQuestion()" class="hidden w-full bg-slate-800 text-white py-3 rounded-xl font-bold shadow-lg hover:bg-slate-700 active:scale-95 transition-transform">下一題</button>
            </div>

            <!-- VIEW: Result -->
            <div id="view-result" class="hidden flex flex-col items-center justify-center h-full text-center py-8 animate-pop">
                <div class="w-24 h-24 bg-yellow-100 rounded-full flex items-center justify-center mb-4 relative">
                    <i data-lucide="trophy" class="w-12 h-12 text-yellow-500"></i>
                    <div class="absolute -top-2 -right-2 bg-red-500 text-white text-xs font-bold px-2 py-1 rounded-full shadow">+<span id="result-points">0</span> pt</div>
                </div>
                <h2 class="text-2xl font-bold text-slate-800 mb-1">練習完成！</h2>
                <p class="text-slate-500 mb-8">你完成了今天的練習</p>

                <div class="bg-white p-6 rounded-2xl shadow-sm border border-slate-100 w-full mb-8">
                    <div class="flex justify-between items-center mb-4 border-b border-slate-100 pb-4">
                        <span class="text-slate-500">答對題數</span>
                        <span class="font-bold text-xl text-green-500"><span id="result-correct">0</span>/5</span>
                    </div>
                    <div class="flex justify-between items-center">
                        <span class="text-slate-500">獲得點數</span>
                        <div class="flex items-center gap-1">
                            <span class="font-bold text-xl text-yellow-500" id="result-points-total">0</span>
                            <i data-lucide="coins" class="w-4 h-4 text-yellow-500"></i>
                        </div>
                    </div>
                </div>

                <div class="flex gap-3 w-full">
                    <button onclick="app.showHome()" class="flex-1 bg-slate-100 text-slate-600 py-3 rounded-xl font-bold hover:bg-slate-200 transition-colors">回首頁</button>
                    <button onclick="app.startQuiz()" class="flex-1 bg-slate-800 text-white py-3 rounded-xl font-bold hover:bg-slate-700 shadow-lg shadow-slate-200 transition-colors">再練一次</button>
                </div>
            </div>

            <!-- VIEW: Shop -->
            <div id="view-shop" class="hidden h-full flex flex-col">
                <div class="flex items-center gap-2 mb-6 cursor-pointer hover:text-pink-500 transition-colors" onclick="app.showHome()">
                    <i data-lucide="arrow-left" class="w-5 h-5"></i>
                    <span class="font-bold">返回首頁</span>
                </div>
                
                <div class="bg-gradient-to-r from-pink-500 to-purple-500 p-6 rounded-2xl text-white mb-6 shadow-lg relative overflow-hidden">
                    <div class="absolute -right-6 -bottom-6 bg-white opacity-20 w-32 h-32 rounded-full"></div>
                    <h3 class="font-bold text-lg mb-1">福利社</h3>
                    <p class="text-pink-100 text-sm opacity-90">用你的努力換取獎勵吧！</p>
                    <div class="mt-4 flex items-center gap-2 bg-white/20 px-3 py-1 rounded-lg w-fit backdrop-blur-sm">
                        <i data-lucide="coins" class="w-4 h-4 text-yellow-300"></i>
                        <span class="font-bold" id="shop-balance">0</span>
                    </div>
                </div>

                <h4 class="font-bold text-slate-700 mb-4 flex items-center gap-2">
                    <i data-lucide="sticker" class="w-4 h-4"></i>
                    可愛貼紙圖鑑
                </h4>
                
                <div id="shop-grid" class="grid grid-cols-2 gap-4 pb-8">
                    <!-- Shop Items Injected Here -->
                </div>
            </div>

        </main>
    </div>

    <script>
        // --- DATA: Questions derived from the provided document ---
        const rawQuestions = [
            {
                q: "論語選三則中，〈譬如為山〉一篇的主旨主要在說明什麼？",
                options: ["學習態度", "孝順的真義", "自我反省", "朋友互助"],
                a: 0, // A
                exp: "〈譬如為山〉以堆土成山為喻，說明為學成敗在於自己是否能持之以恆，屬於學習態度的範疇。"
            },
            {
                q: "「像陳年老酒，越陳越香。」這句話用來譬喻什麼最恰當？",
                options: ["憤怒", "歲月", "好友", "書本"],
                a: 2, // C
                exp: "老朋友就像陳年老酒，時間越久感情越深厚。"
            },
            {
                q: "背影的作者回想往事說：「唉！我現在想想，那時真是太聰明了！」這句話表達了什麼心情？",
                options: ["自信滿滿", "後悔自責", "低落消沉", "擔心焦急"],
                a: 1, // B
                exp: "這是一種「倒反」修辭，作者自嘲當時自作聰明，實則對未能體會父親深情感到後悔自責。"
            },
            {
                q: "杏林子在〈心囚〉一文中提到，真正能囚禁人的是什麼？",
                options: ["身體的病痛", "心理的不健全", "監獄的圍牆", "缺乏金錢"],
                a: 1, // B
                exp: "作者認為身體的殘疾是「身囚」，但如果心理健康，就擁有自由；反之，心理不健全才是真正的「心囚」。"
            },
            {
                q: "子游問孝。子曰：「今之孝者，是謂能養。至於犬馬，皆能有養。」孔子強調孝順的關鍵在於？",
                options: ["豐厚的物質", "順從父母", "恭敬的態度", "揚名顯親"],
                a: 2, // C
                exp: "孔子用犬馬也能得到飼養來對比，強調人若對父母沒有「敬」意，就與飼養動物無異。"
            },
            {
                q: "下列詞語，哪一個與「禍不單行」的意思最接近？",
                options: ["雪上加霜", "滔天大禍", "如喪考妣", "不測之禍"],
                a: 0, // A
                exp: "「禍不單行」指不幸的事接連發生，「雪上加霜」比喻災害接連而來，損害更加嚴重。"
            },
            {
                q: "下列哪一個選項使用了「倒反」修辭？",
                options: ["他氣得火冒三丈", "這真是太棒了，剛出門就踩到狗屎", "光陰似箭，歲月如梭", "他瘦得像一根竹竿"],
                a: 1, // B
                exp: "明明踩到狗屎是壞事，卻說「太棒了」，這是言不由衷的倒反修辭。"
            },
            {
                q: "注音測驗：「銬」手的刑具，讀音為何？",
                options: ["ㄎㄠˋ", "ㄎㄠˇ", "ㄎㄠ", "ㄐㄧㄠˇ"],
                a: 0, // A (ㄎㄠˋ)
                exp: "銬：ㄎㄠˋ，如手銬。"
            },
            {
                q: "下列成語「　」中的字，何組讀音相同？",
                options: ["囑」託／「矚」目", "「柵」欄／「珊」瑚", "慘「澹」／「贍」養", "棉「袍」／「刨」冰"],
                a: 0, // A
                exp: "囑(ㄓㄨˇ)託 / 矚(ㄓㄨˇ)目。兩者讀音相同。"
            },
            {
                q: "「一扇門關上時，另一扇卻開了...」這句話主要指出人經常犯什麼樣的錯誤？",
                options: ["囿於所見", "作繭自縛", "自欺欺人", "猶豫不決"],
                a: 0, // A
                exp: "人們常盯著關閉的門（失去的機會），而忽略了開啟的門（新的機會），是被自己的眼界所限制（囿於所見）。"
            },
            {
                q: "《背影》一文中，作者看到父親買橘子的背影而流淚，主要原因為何？",
                options: ["氣父親違反交通規則", "擔心父親身體不好", "感動於父親艱難的付出", "想到自己沒有橘子吃"],
                a: 2, // C
                exp: "父親肥胖、行動不便，仍努力爬月台買橘子，作者看到此景感受到深沉的父愛而感動落淚。"
            },
            {
                q: "下列何者不是《心囚》作者所謂的「心囚」？",
                options: ["表面歡樂內心痛苦", "追求名利迷失自我", "身體殘疾樂觀度日", "行動自如卻無方向"],
                a: 2, // C
                exp: "身體殘疾但樂觀度日，是「身囚心不囚」，並非作者所指的「心囚」。"
            },
            {
                q: "「譬如平地，雖覆一簣，進，吾往也。」這句話強調學習應該如何？",
                options: ["半途而廢", "持之以恆", "依賴他人", "好高騖遠"],
                a: 1, // B
                exp: "這句話意思是：就像填平窪地，雖然只倒了一筐土，但只要繼續努力，就能成功。強調持之以恆。"
            },
            {
                q: "《論語》一書的主要編輯者是誰？",
                options: ["孔子本人", "孟子", "孔子弟子與再傳弟子", "魯國國君"],
                a: 2, // C
                exp: "《論語》是由孔子的弟子及再傳弟子記錄編輯而成，非孔子親筆著作。"
            },
            {
                q: "成語「瞠目結舌」的意思是？",
                options: ["形容心情快活", "形容十分驚慌害怕", "形容吃驚受窘說不出話", "形容憤怒到極點"],
                a: 2, // C
                exp: "瞠目：瞪大眼睛。結舌：舌頭打結。形容受驚或受窘以致說不出話來。"
            }
        ];

        // --- SHOP DATA ---
        const shopItems = [
            { id: 'sticker_cat', name: '學霸貓貓', icon: '🐱', cost: 50, color: 'bg-orange-100 text-orange-600' },
            { id: 'sticker_star', name: '滿分星星', icon: '⭐', cost: 100, color: 'bg-yellow-100 text-yellow-600' },
            { id: 'sticker_rocket', name: '進步火箭', icon: '🚀', cost: 150, color: 'bg-blue-100 text-blue-600' },
            { id: 'sticker_crown', name: '國文小老師', icon: '👑', cost: 300, color: 'bg-purple-100 text-purple-600' },
            { id: 'sticker_book', name: '論語大師', icon: '📜', cost: 500, color: 'bg-amber-100 text-amber-700' },
            { id: 'sticker_heart', name: '愛心滿滿', icon: '💖', cost: 800, color: 'bg-pink-100 text-pink-600' }
        ];

        // --- APP LOGIC ---
        const app = {
            data: {
                points: 0,
                unlockedItems: [],
                totalCorrect: 0,
                currentQuiz: [], // Array of question indices
                quizIndex: 0,
                quizScore: 0,
                quizCorrectCount: 0
            },

            init() {
                this.loadData();
                this.updateUI();
                lucide.createIcons();
                this.renderShop();
                this.showHome();
            },

            loadData() {
                const saved = localStorage.getItem('chineseQuizData');
                if (saved) {
                    const parsed = JSON.parse(saved);
                    this.data.points = parsed.points || 0;
                    this.data.unlockedItems = parsed.unlockedItems || [];
                    this.data.totalCorrect = parsed.totalCorrect || 0;
                }
            },

            saveData() {
                localStorage.setItem('chineseQuizData', JSON.stringify({
                    points: this.data.points,
                    unlockedItems: this.data.unlockedItems,
                    totalCorrect: this.data.totalCorrect
                }));
                this.updateUI();
            },

            updateUI() {
                document.getElementById('score-display').innerText = this.data.points;
                document.getElementById('shop-balance').innerText = this.data.points;
                document.getElementById('total-correct').innerText = this.data.totalCorrect;
                document.getElementById('unlocked-count').innerText = `${this.data.unlockedItems.length}/${shopItems.length}`;
            },

            // --- NAVIGATION ---
            hideAllViews() {
                ['view-home', 'view-quiz', 'view-result', 'view-shop'].forEach(id => {
                    document.getElementById(id).classList.add('hidden');
                });
            },

            showHome() {
                this.hideAllViews();
                document.getElementById('view-home').classList.remove('hidden');
                document.getElementById('view-home').classList.add('animate-pop');
            },

            showShop() {
                this.hideAllViews();
                document.getElementById('view-shop').classList.remove('hidden');
                this.renderShop();
            },

            // --- QUIZ LOGIC ---
            startQuiz() {
                // Pick 5 random questions
                const indices = Array.from({length: rawQuestions.length}, (_, i) => i);
                // Shuffle
                for (let i = indices.length - 1; i > 0; i--) {
                    const j = Math.floor(Math.random() * (i + 1));
                    [indices[i], indices[j]] = [indices[j], indices[i]];
                }
                this.data.currentQuiz = indices.slice(0, 5);
                this.data.quizIndex = 0;
                this.data.quizScore = 0;
                this.data.quizCorrectCount = 0;

                this.hideAllViews();
                document.getElementById('view-quiz').classList.remove('hidden');
                this.renderQuestion();
            },

            renderQuestion() {
                const qIndex = this.data.currentQuiz[this.data.quizIndex];
                const qData = rawQuestions[qIndex];
                
                // Reset UI
                document.getElementById('q-current').innerText = this.data.quizIndex + 1;
                document.getElementById('progress-bar').style.width = `${((this.data.quizIndex) / 5) * 100}%`;
                document.getElementById('q-text').innerText = qData.q;
                
                // Hide feedback, show options
                document.getElementById('feedback-area').classList.add('hidden');
                document.getElementById('next-btn').classList.add('hidden');
                
                const optionsContainer = document.getElementById('options-container');
                optionsContainer.innerHTML = '';

                qData.options.forEach((opt, idx) => {
                    const btn = document.createElement('button');
                    btn.className = "w-full text-left p-4 rounded-xl border-2 border-slate-100 hover:border-pink-300 hover:bg-pink-50 transition-all font-medium text-slate-700 flex items-center gap-3 group";
                    btn.onclick = () => this.handleAnswer(idx, btn);
                    
                    const letter = ["A", "B", "C", "D"][idx];
                    btn.innerHTML = `
                        <span class="bg-slate-100 text-slate-500 w-8 h-8 rounded-full flex items-center justify-center font-bold text-sm group-hover:bg-pink-200 group-hover:text-pink-600 transition-colors">${letter}</span>
                        <span>${opt}</span>
                    `;
                    optionsContainer.appendChild(btn);
                });
            },

            handleAnswer(selectedIndex, btnElement) {
                // Disable all buttons
                const buttons = document.getElementById('options-container').querySelectorAll('button');
                buttons.forEach(b => {
                    b.disabled = true;
                    b.classList.add('opacity-60', 'cursor-not-allowed');
                });

                const qIndex = this.data.currentQuiz[this.data.quizIndex];
                const qData = rawQuestions[qIndex];
                const isCorrect = selectedIndex === qData.a;

                // Visual feedback on button
                btnElement.classList.remove('opacity-60', 'border-slate-100', 'hover:border-pink-300', 'hover:bg-pink-50');
                
                const feedbackTitle = document.getElementById('feedback-title');
                const feedbackDesc = document.getElementById('feedback-desc');
                const feedbackIcon = document.getElementById('feedback-icon');
                const feedbackArea = document.getElementById('feedback-area');

                if (isCorrect) {
                    btnElement.classList.add('bg-green-100', 'border-green-400', 'text-green-800');
                    this.data.quizScore += 10; // 10 points per question
                    this.data.quizCorrectCount++;
                    this.data.totalCorrect++;
                    
                    feedbackTitle.innerText = "答對了！太棒了！🎉";
                    feedbackTitle.className = "font-bold text-green-600";
                    feedbackIcon.innerHTML = '<i data-lucide="check-circle" class="text-green-500 w-6 h-6"></i>';
                    
                    // Small confetti
                    confetti({
                        particleCount: 30,
                        spread: 50,
                        origin: { y: 0.7 }
                    });
                } else {
                    btnElement.classList.add('bg-red-100', 'border-red-400', 'text-red-800');
                    // Highlight correct answer
                    buttons[qData.a].classList.remove('opacity-60', 'border-slate-100');
                    buttons[qData.a].classList.add('bg-green-50', 'border-green-400', 'text-green-700');

                    feedbackTitle.innerText = "哎呀，答錯了！";
                    feedbackTitle.className = "font-bold text-red-500";
                    feedbackIcon.innerHTML = '<i data-lucide="x-circle" class="text-red-500 w-6 h-6"></i>';
                }

                // Show Explanation
                feedbackDesc.innerText = qData.exp;
                feedbackArea.classList.remove('hidden');
                lucide.createIcons();

                // Show Next Button
                document.getElementById('next-btn').classList.remove('hidden');
            },

            nextQuestion() {
                this.data.quizIndex++;
                if (this.data.quizIndex >= 5) {
                    this.endQuiz();
                } else {
                    this.renderQuestion();
                }
            },

            endQuiz() {
                this.hideAllViews();
                document.getElementById('view-result').classList.remove('hidden');
                
                // Update final points
                this.data.points += this.data.quizScore;
                this.saveData();

                document.getElementById('result-correct').innerText = this.data.quizCorrectCount;
                document.getElementById('result-points').innerText = this.data.quizScore;
                document.getElementById('result-points-total').innerText = this.data.quizScore;

                if (this.data.quizCorrectCount === 5) {
                    setTimeout(() => {
                        confetti({
                            particleCount: 150,
                            spread: 70,
                            origin: { y: 0.6 }
                        });
                    }, 300);
                }
            },

            // --- SHOP LOGIC ---
            renderShop() {
                const grid = document.getElementById('shop-grid');
                grid.innerHTML = '';

                shopItems.forEach(item => {
                    const isUnlocked = this.data.unlockedItems.includes(item.id);
                    const canAfford = this.data.points >= item.cost;
                    
                    const card = document.createElement('div');
                    card.className = `p-4 rounded-xl border-2 flex flex-col items-center gap-2 transition-all relative overflow-hidden ${isUnlocked ? 'bg-white border-slate-100' : 'bg-slate-50 border-slate-200'}`;
                    
                    let btnHtml = '';
                    if (isUnlocked) {
                        btnHtml = `<div class="text-green-500 text-xs font-bold flex items-center gap-1"><i data-lucide="check" class="w-3 h-3"></i> 已擁有</div>`;
                    } else {
                        btnHtml = `
                            <button onclick="app.buyItem('${item.id}')" 
                                class="text-xs px-3 py-1 rounded-full font-bold transition-colors flex items-center gap-1 ${canAfford ? 'bg-slate-800 text-white hover:bg-slate-700' : 'bg-slate-200 text-slate-400 cursor-not-allowed'}" 
                                ${!canAfford ? 'disabled' : ''}>
                                ${item.cost} <i data-lucide="coins" class="w-3 h-3"></i>
                            </button>
                        `;
                    }

                    card.innerHTML = `
                        <div class="w-16 h-16 rounded-full flex items-center justify-center text-3xl mb-1 ${isUnlocked ? item.color : 'bg-slate-200 text-slate-400 grayscale'}">
                            ${item.icon}
                        </div>
                        <div class="font-bold text-slate-700 text-sm">${item.name}</div>
                        ${btnHtml}
                    `;
                    grid.appendChild(card);
                });
                lucide.createIcons();
            },

            buyItem(id) {
                const item = shopItems.find(i => i.id === id);
                if (item && this.data.points >= item.cost) {
                    if (confirm(`確定要花費 ${item.cost} 點數兌換「${item.name}」嗎？`)) {
                        this.data.points -= item.cost;
                        this.data.unlockedItems.push(id);
                        this.saveData();
                        this.renderShop();
                        
                        // Celebration
                        confetti({
                            particleCount: 100,
                            spread: 60,
                            origin: { y: 0.7 },
                            colors: ['#FF69B4', '#FFD700']
                        });
                    }
                }
            }
        };

        // Start App
        app.init();

    </script>
</body>
</html>
